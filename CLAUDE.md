# Project notes for Claude Code

This is Efe Karavul's personal Python/data-science learning portfolio (solo repo, GitHub remote `efekaravul/Applied-Data-Engineering-And-Machine-Learning`).

## Git & Kaggle — standing authorization

- Committing and running `git push origin master` in this repo do **not** need confirmation — just do it directly when asked to commit/push.
- Publishing/updating notebooks as public kernels on Kaggle (account `efekaravul`) does **not** need confirmation either.
- Still ask first for anything hard to reverse or destructive: force-push, `git reset --hard`, deleting branches, deleting the GitHub repo, or deleting/making a Kaggle kernel private after it was public.
- Do not add a `Co-Authored-By: Claude` trailer to commit messages in this repo — commits should show only Efe Karavul as the author.
